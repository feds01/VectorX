package drawing.tool;

/**
 * Enum that holds the registered tool types for the application.
 *
 * @author 200008575
 * */
public enum ToolType {
    SELECTOR,
    FILL,
    LINE,
    TRIANGLE,
    RECTANGLE,
    ELLIPSE,
    IMAGE,
    TEXT
}
